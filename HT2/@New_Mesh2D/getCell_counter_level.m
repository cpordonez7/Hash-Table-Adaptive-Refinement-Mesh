function [Cell_counter_level] = getCell_counter_level(HTObj)
    %This function return number of cell that the HT have in this moment
    %for level

    Cell_counter_level = HTObj.Cell_counter_level;

end